#!/usr/bin/env ruby

require 'i18n'
require 'shanty'

ROOT = File.expand_path(File.join(File.dirname(__FILE__), '..'))
I18n.load_path = Dir[File.join(ROOT, 'lib', 'shanty', 'translations', '*.yml')]

Shanty::Cli.new(Shanty::Shanty.new.graph).run
